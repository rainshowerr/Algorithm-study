#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int n;
    cin >> n;
    if (n == 0) cout << "YONSEI";
    else if (n == 1) cout << "Leading the Way to the Future";
}